import type { Metadata } from "next"

export const metadata: Metadata = {
  title: "חוזה קבלן משנה / ספק",
  description: "Back-to-Back מול חוזה מזמין וניתוח מסמכים עם HR Agent",
}

export default function CreateSubcontractorContractLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children
}
