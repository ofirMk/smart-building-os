import type { ReactNode } from "react"

export const metadata = { title: "חשבון התקדמות חדש" }

export default function ClientProgressBillNewLayout({ children }: { children: ReactNode }) {
  return <>{children}</>
}
