"use server"

export { tendersTakeoffPlaceholder } from "@/lib/marker-ofek/ai/tenders/takeoff-actions"

/**
 * מכרזים — נקודות הרחבה: Takeoff (`takeoff-actions`), השוואת הצעות, סיכונים.
 */
export async function tendersAiPlaceholder(): Promise<{ ok: true }> {
  return { ok: true }
}
