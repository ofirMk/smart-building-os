/**
 * Fixed mirror-mode banner height — dashboard shell, sidebar, and sticky header must stay aligned (`h-8` = 32px).
 */

/** Tailwind `h-8` = 2rem — use for the fixed banner bar */
export const MIRROR_BANNER_H_CLASS = "h-8"

/** Push content below the fixed banner */
export const MIRROR_BANNER_INSET_PT_CLASS = "pt-8"

/** Sticky sub-header offset when banner is visible */
export const MIRROR_BANNER_STICKY_TOP_CLASS = "top-8"
