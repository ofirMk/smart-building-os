/**
 * Infrastructure layer (Diamond V1.0): navigation helpers, typed DB access, cross-cutting utilities.
 * Prefer importing from specific modules (e.g. `lib/infrastructure/navigation/sidebar-routes`) in new code.
 */

export * from "@/lib/infrastructure/navigation/sidebar-routes"
