import "server-only"

/**
 * Placeholder for journal entry persistence (public.journal_entries / journal_lines).
 * No queries run here yet — avoids accidental imports from client or Server Action misuse.
 */
