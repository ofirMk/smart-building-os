# Legacy ERP Data
Drop all legacy CSV files (Items, Suppliers, Customers, Projects, Journal Entries) into this folder. 
These files will be ingested by the `import-legacy-data.ts` script to populate the sandbox database.
